name: Test

on: 
 pull_request:
  types: [labeled,synchronize,unlabeled,edited]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: powers2537/bump-package-version-sample@v1
        with:
          path: 'right_here'